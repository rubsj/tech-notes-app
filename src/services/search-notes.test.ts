//import { searchNotes } from './search-notes';

describe('test searchNotes', () => {
  it('should return notes ', async () => {
    //TBD write tests using msw
    //const result = await searchNotes(['d23a']);
  //  expect(result).toBeTruthy();
  });
});
